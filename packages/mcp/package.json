{
  "name": "@lexdiff/mcp",
  "version": "0.1.0",
  "description": "B-4: MCP server — lexdiff_what_changed tool for AI agents",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "lexdiff-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch dist/index.js",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@lexdiff/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
